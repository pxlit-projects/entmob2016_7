<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FitSense.Views.ExercisePage">

  
  <ContentPage.Content>
    <StackLayout Spacing="10" x:Name="layout">
      <Label Text="{Binding Category.Name}" HorizontalOptions="Center" FontSize="Large" />
      <Label Text="{Binding Exercise.Name}" VerticalOptions="Start" HorizontalOptions="Center" />
      <Label Text="{Binding Exercise.Description}" VerticalOptions="FillAndExpand" HorizontalOptions="Center" />
      <Button Text="Sets" Command="{Binding GoToSetCarouselCommand}" VerticalOptions="FillAndExpand" HorizontalOptions="Center"/>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>